# Monsieur Chalets single page

1. **## Install dependencies**
   ```bash
   npm install
   ```
2. **## Development Build**  
   Start the Tailwind CLI build:

```bash
npx tailwindcss -i ./src/input.css -o ./src/output.css --watch
```
